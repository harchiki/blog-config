server:
  port: 8761

eureka:
  hostname: localhost
  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://${eureka.hostname}:${server.port}/eureka/